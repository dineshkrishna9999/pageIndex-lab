# LLM config for LiteLLM / Azure OpenAI (or change model_name for another provider)
# ADK uses the Azure Responses API → api_version must be 2025-03-01-preview or later
llm={"model_name": "azure/gpt-4.1", "provider_args": {"api_key": "", "api_base": "https://your-endpoint.openai.azure.com", "api_version": "2025-03-01-preview"}}
